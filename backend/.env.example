# Database configuration ------------------------------------------------------
# Point the backend at your local PostgreSQL instance. For dev on Windows
# (PostgreSQL 18, data dir H:\apps\pgsql\data), these values should match
# the local connection details:
#   Host: 127.0.0.1
#   Port: 5432
#   Database: toofunny
#   User: postgres
#   SSL: disable
#
# Use either the full connection string or the discrete values below.
DATABASE_URL=postgresql://postgres:<PASSWORD>@127.0.0.1:5432/toofunny?sslmode=disable
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=toofunny
DB_USER=postgres
DB_PASSWORD=<PASSWORD>
DB_SSLMODE=disable
SESSION_SECRET=
ALLOWLIST_EMAILS=
FRONTEND_URL=http://localhost:5173
CORS_ORIGIN=http://localhost:5173
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Leave as a relative path to auto-detect the host. If you override with a full URL,
# make sure it matches your public domain (never keep http://localhost in production).
GOOGLE_CALLBACK_URL=/api/auth/google/callback
PORT=5000
