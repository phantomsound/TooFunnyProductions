# Point the React dev server at the local API.
VITE_API_URL=http://localhost:5000

# Supabase/PostgREST browser configuration. Keep this aligned with backend/.env.
#   While Supabase hosts production, leave these pointed at the cloud project.
#   After the local PostgreSQL migration passes validation, swap to the
#   PostgREST endpoint that fronts your local database (for example
#   `http://127.0.0.1:54321` when using the Supabase CLI stack).
#   Once Supabase is retired, rotate the anon key so only the local endpoint
#   remains accessible.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
