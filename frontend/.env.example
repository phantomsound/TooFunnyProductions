# Point the React dev server at the local API.
VITE_API_URL=http://localhost:5000

# Supabase/PostgREST (MikoDB) browser configuration.
#   If you want the frontend to talk directly to MikoDB/PostgREST (for auth
#   helpers or realtime), point these at the same local gateway the backend
#   uses. Leaving them empty keeps the browser on the backend API only.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<LOCAL_ANON_KEY>

# Optional: minutes of inactivity before the admin UI signs the user out.
VITE_ADMIN_TIMEOUT_MINUTES=30
