# Point the React dev server at the local API.
VITE_API_URL=http://localhost:5000

# Supabase/PostgREST browser configuration.
#   Leave these empty for local development so the frontend only talks to the
#   backend API. Populate them only if you intentionally expose PostgREST to
#   the browser (not recommended for local dev).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
